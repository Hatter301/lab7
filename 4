#include <iostream>
using namespace std;
int main()
{
    int a,x,y,z;
    cin>>a;
    x=a/100;
    y=(a%100)/10;
    z=(a%10);
    if (((x>y)&&(y>z))||((z>y)&&(y>x)))
    cout<<"Истинно";
    else cout<<"Ложно";
    return 0;
}
