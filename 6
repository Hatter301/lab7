#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int a,b,c;
    cin>>a>>b>>c;
    if ((sqrt(a*a+b*b)==c)||(sqrt(c*c+b*b)==a)||(sqrt(c*c+a*a)==b))
    cout<<"Истинно";
    else cout<<"Ложно";
    return 0;
}
